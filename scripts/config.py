""" 
config.py

Central config for database path — used by all scripts for consistency
"""

# Path to the SQLite database file
DB_FILE = "data/songs.db"

# Path to the SQL schema definition
SCHEMA_FILE = "scripts/schema.sql"
